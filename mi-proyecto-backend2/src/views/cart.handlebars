<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Venta de Productos</title>
    <link rel="stylesheet" href="/css/cart.css" />
</head>

<body>
    {{> header}}
    <div>
        <h3>Lista de productos de un carrito:</h3>

        {{#each carrito.products}}
        <div>
            <h4>Nombre del producto:
                <span>{{this.id.title}}</span>
            </h4>
            <p>Descripción:
                {{this.id.description}}</p>
            <p>Precio:
                <span>${{this.id.price}}</span>
            </p>
            <p>Cantidad: {{this.quantity}}</p>
        </div>
        {{/each}}
    </div>

</body>

</html>