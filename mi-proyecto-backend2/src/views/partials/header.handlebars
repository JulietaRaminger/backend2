<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}}</title>
    <style>
        /* Estilos generales para el header */
        header {
            background-color: #000;
            /* Fondo negro */
            color: #f5f5f5;
            /* Texto gris claro */
            padding: 15px 0;
            border-bottom: 1px solid #333;
            /* Borde inferior gris oscuro */
        }

        .header-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        h1 {
            font-size: 24px;
            color: #f5f5f5;
            /* Texto gris claro */
            margin: 0;
        }

        .navbar {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .nav-link {
            color: #f751c5;
            /* Color rosa para los enlaces */
            text-decoration: none;
            padding: 8px 15px;
            border-radius: 5px;
            transition: background-color 0.3s, color 0.3s;
            font-weight: 500;
        }

        .nav-link:hover {
            background-color: #f751c5;
            /* Color rosa en hover */
            color: #fff;
            /* Texto blanco en hover */
        }

        .welcome-message {
            margin: 0;
            font-size: 18px;
            color: #f5f5f5;
            /* Texto gris claro */
        }

        .avatar-de-github {
            display: flex;
            align-items: center;
        }

        .avatar-de-github img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            border: 2px solid #f751c5;
            /* Borde rosa alrededor del avatar */
            margin-left: 10px;
        }
    </style>
</head>

<body>
    <header>
        <div class="header-container">
            <h1>{{title}}</h1>
            <nav class="navbar">
                {{#if user}}
                <p class="welcome-message">Bienvenido, {{user.email}}</p>
                <a href="/logout" class="nav-link">Cerrar sesión</a>

                {{#if user.avatar}}
                <div class="avatar-de-github">
                    <img src="{{user.avatar}}" alt="Imagen de usuario de github" />
                </div>
                {{/if}}
                {{else}}
                <a href="/login" class="nav-link">Iniciar sesión</a>
                <a href="/register" class="nav-link">Registrarse</a>
                {{/if}}
                <a href="/chat" class="nav-link">Chat</a>
                <a href="/realtimeproducts" class="nav-link">Productos en Tiempo Real</a>
                <a href="/cart/66db20cc9ede0445750f3bd8" class="nav-link">Carrito</a>
                <a href="/products" class="nav-link">Productos</a>
            </nav>
        </div>
    </header>
</body>

</html>